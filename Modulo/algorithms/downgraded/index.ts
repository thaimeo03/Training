export class ModDownGraded {
  private a: number
  private m: number
  private n: number

  constructor({a, m, n}: {a: number, m: number, n: number}) {
    this.a = a
    this.m = m
    this.n = n
  }

  cal() {
    
  }
}